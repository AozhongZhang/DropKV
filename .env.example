# Copy this file to .env and edit the paths for your machine.
# All paths can also be overridden via CLI flags in run_eval.sh.

# Path to the HuggingFace model (local directory or Hub ID)
MODEL_PATH=meta-llama/Llama-3.1-8B-Instruct

# Directory where lm-eval writes results (optional)
OUTPUT_DIR=./results
